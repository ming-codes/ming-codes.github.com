<div id="blog">
  {{#each model as |post|}}
    <article>
      <time>{{moment-format post.published "MMMM DD, YYYY"}}</time>
      <h1>{{post.title}}</h1>

      {{{post.content}}}
    </article>
  {{/each}}
</div>